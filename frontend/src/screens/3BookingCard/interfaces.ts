export interface Conveniences {
  wifi: boolean,
  bedLinen: boolean,
  airConditioner: boolean,
  tv: boolean,
  towels: boolean,
  hairDryer: boolean,
  pool: boolean
}

export interface Rules {
  childRules: boolean,
  smokingRules: boolean,
  petRules: boolean,
  partyRules: boolean
}